set showmode    # Ensures we see when we are in insert mode 
set nohlsearch  # Does not highlight the words that we have searched for
set autoindent  # Allows to return to the last indent level rather than the start of a new line on each line break
set tabstop=4   # Sets a tab to be four spaces
set expandtab   # Converts tabs to spaces, avoids errors when the file moves to other system
syntax on       # Turns on syntax highlighting 
